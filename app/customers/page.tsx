"use client";import{useEffect,useState}from"react";import Link from"next/link";export default function Customers(){const[c,setC]=useState<any[]>([]),[name,setN]=useState(""),[phone,setP]=useState("");const load=()=>fetch("/api/data?collection=customers").then(r=>r.json()).then(setC);useEffect(load,[]);async function add(){if(!name)return;await fetch("/api/data",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({collection:"customers",name,phone})});setN("");setP("");load()}return <main className="shell"><div className="nav"><Link href="/">← Home</Link><Link href="/pos">New Sale</Link></div><h1>👥 Customers & Ledger</h1><div className="panel"><div className="row"><input className="input col" value={name} onChange={e=>setN(e.target.value)} placeholder="Customer Name"/><input className="input col" value={phone} onChange={e=>setP(e.target.value)} placeholder="Mobile"/><button className="btn green" onClick={add}>Save Customer</button></div></div><div className="panel"><table className="table"><thead><tr><th>Name</th><th>Phone</th><th>ID</th></tr></thead><tbody>{c.map(x=><tr key={x._id}><td>{x.name}</td><td>{x.phone}</td><td>{x._id.slice(-6)}</td></tr>)}</tbody></table></div></main>}
