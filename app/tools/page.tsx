 "use client";import{useState}from"react";import Link from"next/link";const cats=["Panel Finder Tool","Universal Charging Board Finder","Display / LCD Finder","Battery Finder","IC Finder","Connector Finder","Flex Cable Finder","Camera Finder","Speaker / Mic Finder","Mobile Model Finder","Part Number Finder"];export default function Tools(){const[c,setC]=useState<string|null>(null),[q,setQ]=useState("");return <main className="shell"><div className="nav"><Link href="/">← Home</Link></div><h1>🔧 Universal Finder Tool by Search</h1>{!c?<div className="grid">{cats.map(x=><button className="card" style={{textAlign:"left",color:"#fff"}} onClick={()=>setC(x)} key={x}><span className="icon">🔧</span><h2>{x}</h2><p className="muted">Open this finder</p></button>)}</div>:<><button className="btn no-print" onClick={()=>setC(null)}>← All Finder Categories</button><div className="panel"><h2>{c}</h2><input className="input" placeholder="Search model / code / part number..." value={q} onChange={e=>setQ(e.target.value)}/><p className="muted">{q?`Searching ${c} for “${q}”`:"Search to show matching data."}</p></div></>}</main>}