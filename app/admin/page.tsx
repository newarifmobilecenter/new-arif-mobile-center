"use client";import{useEffect,useState}from"react";import Link from"next/link";export default function Admin(){const[p,setP]=useState<any[]>([]),[name,setN]=useState(""),[model,setM]=useState(""),[price,setPr]=useState(""),[stock,setS]=useState("0");const load=()=>fetch("/api/data?collection=products").then(r=>r.json()).then(setP);useEffect(load,[]);async function add(){if(!name||!price)return alert("Name aur price zaroor dein");await fetch("/api/data",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({collection:"products",name,model,price:Number(price),stock:Number(stock)})});setN("");setM("");setPr("");setS("0");load()}return <main className="shell"><div className="nav"><Link href="/">← Home</Link><Link href="/pos">New Sale</Link></div><h1>📦 Products & Stock</h1><div className="panel"><div className="row"><input className="input col" placeholder="Product Name" value={name} onChange={e=>setN(e.target.value)}/><input className="input col" placeholder="Model / Part No." value={model} onChange={e=>setM(e.target.value)}/><input className="input col" type="number" placeholder="Sale Price" value={price} onChange={e=>setPr(e.target.value)}/><input className="input col" type="number" placeholder="Opening Stock" value={stock} onChange={e=>setS(e.target.value)}/><button className="btn green" onClick={add}>Save Product</button></div></div><div className="panel"><table className="table"><thead><tr><th>Product</th><th>Model</th><th>Price</th><th>Stock</th></tr></thead><tbody>{p.map(x=><tr key={x._id}><td>{x.name}</td><td>{x.model}</td><td>Rs. {x.price}</td><td>{x.stock}</td></tr>)}</tbody></table></div></main>}